BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[assets] DROP CONSTRAINT [assets_asset_id_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
